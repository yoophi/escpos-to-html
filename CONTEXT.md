# ESC/POS Receipt Emulator

ESC/POS 바이트 스트림을 수신해 영수증으로 해석하고 화면에서 검증하는 단일 컨텍스트다.

## 영수증 수신

**영수증(Receipt)**:
사용자에게 하나의 출력물로 보이는 완료된 ESC/POS 바이트 묶음과 그 해석 결과다.
_Avoid_: 메시지, 패킷

**영수증 프레임(Receipt Frame)**:
컷 명령, 유휴 시간 초과, 또는 연결 종료로 완료가 판정된 연속 바이트 구간이다. TCP의 개별 전송 단위와 같지 않다.
_Avoid_: chunk, 메시지

**영수증 세션(Receipt Session)**:
데스크탑 앱에서 현재 보관·선택·표시하는 수신 영수증 집합과 그 상태다.
_Avoid_: 히스토리, 캐시

## 영수증 표시

**영수증 레이아웃(Receipt Layout)**:
영수증의 텍스트, 이미지, 바코드를 선택된 용지 조건에서 표시하기 위해 계산한 기하다. 개별 renderer의 출력 결과 자체가 아니다.
_Avoid_: renderer 출력, 화면 좌표

**프린터 프로파일(Printer Profile)**:
특정 프린터 또는 용지 조건에서 유효한 출력 가능 폭과 심볼 규칙을 나타내는 값의 집합이다.
_Avoid_: 공통 ESC/POS 제한
